<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:GeoServerDesktop.App.ViewModels"
             mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="700"
             x:Class="GeoServerDesktop.App.Views.StoresManagementView"
             x:DataType="vm:StoresManagementViewModel">

    <Design.DataContext>
        <vm:StoresManagementViewModel/>
    </Design.DataContext>

    <Grid RowDefinitions="Auto,*,Auto">
        <!-- Header -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="15" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid ColumnDefinitions="*,Auto">
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Data Stores" 
                               FontSize="24" 
                               FontWeight="Bold"/>
                    <TextBlock Text="Manage data store connections to vector and raster data sources" 
                               FontSize="13"
                               Foreground="#666666"
                               Margin="0,5,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button Content="+ Add Store"
                            Command="{Binding ShowCreateDialogCommand}"
                            Background="#2196F3"
                            Foreground="White"
                            Padding="12,6"/>
                    <Button Content="Refresh" 
                            Command="{Binding LoadDataStoresCommand}"
                            Padding="12,6"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" ColumnDefinitions="250,*" Margin="10">
            <!-- Workspace Selector -->
            <Border Grid.Column="0" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1" 
                    Padding="10"
                    Margin="0,0,10,0"
                    Background="White"
                    CornerRadius="4">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                        <TextBlock Text="Workspace" 
                                   FontWeight="Bold" 
                                   FontSize="14"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="Select a workspace to view its data stores" 
                                   FontSize="12"
                                   Foreground="#666666"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    
                    <ListBox ItemsSource="{Binding Workspaces}"
                             SelectedItem="{Binding SelectedWorkspace}"
                             Background="Transparent">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8,6" 
                                        Background="Transparent"
                                        CornerRadius="3">
                                    <TextBlock Text="{Binding}" FontSize="13"/>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <!-- Data Stores List + Create Dialog -->
            <Grid Grid.Column="1" RowDefinitions="*,Auto">
                <Border Grid.Row="0"
                        BorderBrush="#E0E0E0" 
                        BorderThickness="1" 
                        Padding="15"
                        Background="White"
                        CornerRadius="4">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,15">
                            <TextBlock Text="Data Stores" 
                                       FontWeight="Bold" 
                                       FontSize="14"
                                       Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding SelectedWorkspace, StringFormat='Workspace: {0}'}" 
                                       FontSize="12"
                                       Foreground="#666666"
                                       IsVisible="{Binding !!SelectedWorkspace}"/>
                        </StackPanel>

                        <!-- Data Store List -->
                        <Grid>
                            <TextBlock Text="Select a workspace to view data stores" 
                                       FontSize="14"
                                       Foreground="#999999"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       IsVisible="{Binding !SelectedWorkspace}"/>

                            <ScrollViewer IsVisible="{Binding !!SelectedWorkspace}">
                                <ItemsControl ItemsSource="{Binding DataStores}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#E0E0E0" 
                                                    BorderThickness="1" 
                                                    Padding="15"
                                                    Margin="0,0,0,10"
                                                    Background="#FAFAFA"
                                                    CornerRadius="4">
                                                <Grid ColumnDefinitions="*,Auto">
                                                    <StackPanel Grid.Column="0" Spacing="5">
                                                        <TextBlock Text="{Binding Name}" 
                                                                   FontWeight="Bold" 
                                                                   FontSize="15"/>
                                                        <TextBlock Text="{Binding Type}" 
                                                                   FontSize="12"
                                                                   Foreground="#666666"/>
                                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                                            <TextBlock Text="Enabled:" 
                                                                       FontSize="11"
                                                                       Foreground="#666666"/>
                                                            <TextBlock Text="{Binding Enabled}" 
                                                                       FontSize="11"
                                                                       Foreground="#4CAF50"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                    
                                                    <StackPanel Grid.Column="1" 
                                                                Orientation="Horizontal" 
                                                                Spacing="5"
                                                                VerticalAlignment="Center">
                                                        <Button Content="Delete" 
                                                                Padding="10,5"
                                                                FontSize="12"
                                                                Background="#F44336"
                                                                Foreground="White"
                                                                CommandParameter="{Binding}"
                                                                Click="OnDeleteDataStoreClick"
                                                                Tag="{Binding}"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </DockPanel>
                </Border>

                <!-- Create DataStore Dialog -->
                <Border Grid.Row="0"
                        IsVisible="{Binding IsCreateDialogVisible}"
                        Background="#80000000"
                        CornerRadius="4">
                    <Border Background="White"
                            BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="20"
                            MaxWidth="400"
                            MaxHeight="260"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Add Data Store" FontSize="16" FontWeight="Bold"/>
                            <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto" HorizontalAlignment="Stretch">
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewDataStoreName}" Watermark="Data store name"/>
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" VerticalAlignment="Center" Margin="0,8,10,0"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NewDataStoreDescription}" Watermark="Optional description" Margin="0,8,0,0"/>
                            </Grid>
                            <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right">
                                <Button Content="Cancel"
                                        Command="{Binding CancelCreateCommand}"
                                        Padding="12,6"/>
                                <Button Content="Create"
                                        Command="{Binding CreateDataStoreCommand}"
                                        Background="#2196F3"
                                        Foreground="White"
                                        Padding="12,6"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Border>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#F5F5F5" Padding="10,8" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid ColumnDefinitions="*,Auto">
                <TextBlock Grid.Column="0" 
                           Text="{Binding StatusMessage}" 
                           VerticalAlignment="Center"
                           FontSize="12"/>
                <ProgressBar Grid.Column="1" 
                             Width="100"
                             Height="4"
                             IsIndeterminate="True"
                             IsVisible="{Binding IsLoading}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
