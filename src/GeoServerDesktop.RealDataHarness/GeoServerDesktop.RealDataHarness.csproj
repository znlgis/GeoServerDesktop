<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <AssemblyName>GeoServerDesktop.RealDataHarness</AssemblyName>
    <RootNamespace>GeoServerDesktop.RealDataHarness</RootNamespace>
    <Version>1.0.0</Version>
  </PropertyGroup>
  <ItemGroup>
    <!-- 复用测试工程的检查逻辑（数据无关：期望值从数据文件头/manifest 独立推导） -->
    <ProjectReference Include="..\GeoServerDesktop.Tests\GeoServerDesktop.Tests.csproj" />
  </ItemGroup>
</Project>
